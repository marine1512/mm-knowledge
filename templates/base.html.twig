<!DOCTYPE html>
<html>
<head>
    <title>{% block title %}KNOWLEDGE{% endblock %}</title>
    {% block stylesheets %}
   <link rel="stylesheet" href="{{ asset('css/style.css') }}?v=1.0">
    {% endblock %}
    <link rel="icon" type="image/x-icon" href="{{ asset('images/Favicon.png') }}">
</head>
    <nav>
    <a href=""><img src="{{ asset('images/Logo.jpg') }}" alt="Logo du site" class="form-image"></a>
        <ul>
            <li><a href="{{ path('home') }}">Accueil</a></li>
            <!-- Afficher un lien vers "Cours" uniquement si l'utilisateur est connecté -->
            {% if app.user %}
                <li><a href="">Formations</a></li>
            {% endif %}
                <li>
                {% if app.user %}
                    <!-- Si l'utilisateur est connecté, afficher "Se déconnecter" -->
                    <a href="{{ path('app_logout') }}">Se déconnecter</a>
                {% else %}
                    <!-- Sinon afficher "Se connecter" -->
                    <a href="{{ path('app_login') }}">Se connecter</a>
                {% endif %}
            </li>
        </ul>


    </nav>
    <main>
       {% block body %}
        {% endblock %}
    </main>
<footer class="site-footer">
    <h3>Knowledge Learning</h3>
            <ul>
            <li><a href="">Accueil</a></li>
            <!-- Afficher un lien vers "Cours" uniquement si l'utilisateur est connecté -->
            {% if app.user %}
                <li><a href="">Formations</a></li>
            {% endif %}
                <li>
                {% if app.user %}
                    <!-- Si l'utilisateur est connecté, afficher "Se déconnecter" -->
                    <a href="{{ path('app_logout') }}">Se déconnecter</a>
                {% else %}
                    <!-- Sinon afficher "Se connecter" -->
                    <a href="{{ path('app_login') }}">Se connecter</a>
                {% endif %}
            </li>
        </ul>

</footer>
</html>
