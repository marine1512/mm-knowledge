{% extends 'base.html.twig' %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="{{ asset('css/admin/admin.css') }}?v=1.0">
{% endblock %}

{% block title %}Liste des cursus{% endblock %}

{% block body %}
<h1>Liste des cursus</h1>

<a href="{{ path('new') }}" class='btn'>Créer un cursus</a>

<table class='table'>
    <thead>
        <tr>
            <th>ID</th>
            <th>Nom</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for cursus in cursus %}
        <tr>
            <td>{{ cursus.id }}</td>
            <td>{{ cursus.nom }}</td>
            <td>
                <a href="{{ path('edit_cursus', { id: cursus.id }) }}"class='btn'>Modifier</a>
                <form method="post" action="{{ path('delete_cursus', { id: cursus.id }) }}" style="display:inline;">
                    <button type="submit" class='btn'>Supprimer</button>
                </form>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

    <button class='btn'><a href="{{ path('admin') }}">Retour au tableau de bord administrateur</a></button>
{% endblock %}